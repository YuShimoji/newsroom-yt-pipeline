"""Adapters for downstream tooling.

M5 component. The ymm4_export module renders a self-contained artifact
bundle that NLMYTGen or any other YMM4-side tool can read as input. Per
DR-006, the integration is schema-only; this package never invokes
NLMYTGen or any other external process.
"""
