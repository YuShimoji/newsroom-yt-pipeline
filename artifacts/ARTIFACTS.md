# Review Artifacts

This manifest lists tracked, reviewable local artifacts introduced for repository inspection or diagnostic use. It is not a runtime export store and does not make any artifact a source-of-truth replacement for the original Markdown, database rows, or operator evidence.

Do not add runtime DBs, generated export bundles, proof screenshots, raw article bodies, OPML dumps, tokens, private traces, `.ymmp` files, or publishing outputs here.

| Artifact ID | Purpose | Identity / source of truth | Access | Validation | Review status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| `local_docs_view` | Browser tree view for Markdown review and page-translation checks. | Manifest: `artifacts/ARTIFACTS.md`; tracked paths: `pyproject.toml`, `mkdocs.yml`, `.docs-view/`, `docs/index.md`, `tools/generate-doc-nav.ps1`, `scripts/operator/open_dashboard.ps1`; source of truth: original repository Markdown reached through checked-in `.docs-view/` snippet wrappers. | Repo-local launcher: `powershell -ExecutionPolicy Bypass -File scripts\operator\open_dashboard.ps1`; fallback open command from repo root: `mkdocs serve --dev-addr 127.0.0.1:8000`, then open `http://127.0.0.1:8000/`. | `python -m mkdocs build --strict`; launcher smoke with `scripts\operator\open_dashboard.ps1 -NoBrowser`. | Local review surface only; original Markdown files remain canonical. | Open through the launcher when browser review or translation aid is needed; do not edit `.docs-view/` as a replacement authority. |
| `critical_source_readback_cli` | Diagnostic readback of DB-recorded critical-view source rows for a story. | Manifest: `artifacts/ARTIFACTS.md`; tracked paths: `src/newsroom/cli/main.py`, `tests/test_critical_sources.py`; source of truth: runtime DB `story_critical_sources` rows plus article metadata, operator note, and recorded timestamp. | Open command from repo root: `python -m newsroom.cli.main --db <db> packet critical-list --story <story_id> --format json`. | `python -m pytest tests\test_critical_sources.py -q` and synthetic temp-DB smoke. | Diagnostic-only; does not adopt sources, rebuild packets, clear warnings, or approve editorial/legal use. | Run against a temp or operator-approved DB when source readback is needed; use `packet add-critical` and rebuild steps only after a separate source decision. |
| `newsroom-cockpit-governance` | Operation Cockpit v1.11 report guidance for routing, stable meters, Turn Calendar, Review Card, action ownership, and Supervisor Relay. | Manifest: `artifacts/ARTIFACTS.md`; tracked paths: `pyproject.toml`, `docs/DEVELOPMENT_PRACTICES.md`, `docs/HANDOFF.md`, `docs/RUNTIME_STATE.md`, `docs/index.md`; source of truth: the Operation Cockpit sections in `docs/DEVELOPMENT_PRACTICES.md` plus the active Current Capsule in `docs/RUNTIME_STATE.md`. | Repo-local launcher: `powershell -ExecutionPolicy Bypass -File scripts\operator\open_dashboard.ps1`; direct readback from repo root: `Get-Content -Raw -Encoding UTF8 docs\DEVELOPMENT_PRACTICES.md`. | `python -m pytest -q`; `git diff --check`; `python -m mkdocs build --strict`; search for non-ASCII chat/report meters before closeout. | Governance/reference artifact only; it standardizes reports and does not approve content, rights, YMM4 geometry, publishing, or runtime outputs. | Use for supervisor-facing reports and closeouts; do not emit a next-Agent prompt unless the Handoff Gate is true. |
| `newsroom-export-fixture-for-nlmytgen` | Fake newsroom export bundle shape and field contract for downstream NLMYTGen readiness probes. | Manifest: `artifacts/ARTIFACTS.md`; tracked paths: `samples/_probe/newsroom_handoff/newsroom_export_fixture_v1.json`, `docs/integration/NEWSROOM_EXPORT_FIXTURE_FOR_NLMYTGEN_V1.md`, `.docs-view/docs/integration/NEWSROOM_EXPORT_FIXTURE_FOR_NLMYTGEN_V1.md`, `tests/test_newsroom_export_fixture.py`; source of truth: fake JSON fixture plus the contract/readback table. | Direct readback from repo root: `Get-Content -Raw -Encoding UTF8 docs\integration\NEWSROOM_EXPORT_FIXTURE_FOR_NLMYTGEN_V1.md`; JSON parse/readback: `python -m pytest tests\test_newsroom_export_fixture.py -q`. | `python -m pytest tests\test_newsroom_export_fixture.py -q`; `git diff --check`; no-real-URL and prohibited-responsibility checks in the focused test. | Fake-only contract probe; no real packet acceptance, rights clearance, production readiness, YMM4 geometry, or publishing approval is implied. | Use this fixture to align NLMYTGen validators before real packet work; replace fake values only after a reviewed newsroom packet exists. |

## Review Input Metadata

| Artifact ID | review_input_mode | review_prompt_hint | user_action |
| --- | --- | --- | --- |
| `local_docs_view` | `freeform` | Say what looks stale, unclear, hard to access, or ready to keep; fixed labels are not required. | `USER_REVIEW_FREEFORM` only when a Review Card asks for docs-view feedback; otherwise user-open-only. |
| `critical_source_readback_cli` | `freeform` | Comment on whether the listed critical-view source is the right target or whether the diagnostic output is enough; fixed labels are not required. | `USER_REVIEW_FREEFORM` only when source review is explicitly requested; otherwise agent-run diagnostic. |
| `newsroom-cockpit-governance` | `freeform` | Say what report section is hard to compare, stale, too verbose, or missing owner/access information; fixed labels are not required. | `USER_REVIEW_FREEFORM` only when a supervisor asks for report-governance feedback; otherwise reference-only. |
| `newsroom-export-fixture-for-nlmytgen` | `freeform` | Comment on missing fields, ownership, failure behavior, or NLMYTGen consumer expectations; fixed labels are not required. | `USER_REVIEW_FREEFORM` only if the downstream contract needs human adjustment; otherwise reference-only. |

## Maintenance Notes

- Keep artifact paths repo-relative so the manifest remains portable across local checkouts.
- If a new tracked review artifact is added, record its purpose, tracked path, run/open command, source, validation command, and review status here.
- For each reviewable artifact, record `review_input_mode: freeform` and a short review prompt hint when user feedback may be requested.
- If a generated artifact is intentionally committed, document why it must be tracked and how to regenerate it. Otherwise keep generated output under ignored runtime directories.
